#include <iostream>
#include "FileHandler.h"
#include "Profile.h"



int main()
{
    // Open data file
    FileHandler file("data.bin");

    //dynamicaly create array

    //pass array ref to file.load()

    //game loop

        //get player choice

        //if search, go through array with binary

        //if edit after search, get new record, then file.update()

        //if new record, get input, then file.newRecord()

        //does user want to continue?

    


    // File goes out of scope and closes the file
    return 0;
}